<dialog id="whitelisted_modal" class="modal">
    <div class="modal-box">
    <h3 class="text-lg font-bold">Игроки в вайтлисте</h3>
    <p class="font-mono"><%= mc.whitelisted.join(`, `) %></p>
    <div class="modal-action">
        <form method="dialog">
        <button class="btn">Закрыть</button>
        </form>
    </div>
    </div>
</dialog>